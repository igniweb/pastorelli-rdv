;(function (app, $) {

    'use strict';

    // Run modules
    app.module(
        'calendar'
    );

    // Run application onDomReady
    app.ready(function () {
        //
    });

})(App, jQuery);
