;(function (app, $) {

    'use strict';

    // Run modules
    app.module(
        'pusher',
        'calendar'
    );

    // Run application onDomReady
    app.ready(function () {
        //
    });

})(App, jQuery);
